We will put configuration variables here that will not be pushed to the version control due to their sensitive nature. 

For Example:
secret key, database URI (which contains the database user password) etc